import styled from 'styled-components';

const Body = styled.div`
  width: 100%;
  overflow-x: hidden;
  
  main.home-content {
    display: flex;
    justify-content: center;
    
    article {
      padding: 10px;
      width: 80%;
    }
  }
`;

export default Body;